/**
 * Duck szn's noises now live in the shared sound bank - every one of them is
 * still here by name, this is just where they moved to when a second game
 * needed synthesised sound too.
 */
export { isMuted, play, setMuted, type Sound } from '../../lib/audio'
